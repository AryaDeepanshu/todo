<div class="container">
    <header class="header">
            <a href="/" style="text-decoration:none;"><div class="logo">Taskly</div></a>
            <nav class="nav">
                <% if(details){ %> 
                    <p>Welcome <%= details %></p>
                    <a href="/logout">Logout</a>
                    <% }else{ %>
                        <a href="/login">Login</a>
                        <a href="/signup">Signup</a>
                    <% } %>
            </nav>
    </header>
</div>